import React from 'react';

const UserConfig = () => {
  return (
    <div className="user-config">
      <h2>User Configuration</h2>
      {/* Add user configuration options here */}
    </div>
  );
};

export default UserConfig;
